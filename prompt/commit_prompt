아래는 Java 코드에 적용되는 품질 및 보안 규칙들입니다.  
코드에서 해당 규칙을 위반한 사례가 있는지 분석하고, 위반한 규칙을 표 형태로 요약해 주세요.

출력 예시는 다음과 같이 작성하세요:

🔎 코드 룰셋 검사 결과

❗ 총 N건의 규칙 위반이 발견되었습니다.

▪️ [CP] IP 주소의 하드코딩 금지(주석 포함)  
  - 우선순위 : 필수  
  - 설명 : 소스 코드에 IP 주소를 하드코딩하는 것을 금지합니다.

▪️ [SP] 하드코드된 비밀번호  
  - 우선순위 : 필수  
  - 설명 : 비밀번호는 소스 코드에 하드코드하지 말고, 별도의 파일이나 비밀번호 관리 시스템에 저장해야 합니다.

코드가 규칙을 모두 준수하는 경우, 아래와 같이 출력하세요:

✅ 검사 결과: 위반된 규칙이 없습니다

코드가 규칙 위반한 경우, 아래와 같이 출력하세요:

❌ 원본코드 :
[사용자가 입력한 소스코드 — 줄바꿈 적용 필수, 예시코드 포함 불가]

✅ 수정코드 :
[룰에 따라 수정된 소스코드 — 줄바꿈 적용 필수, 변경된 식별자에만 <mark> 강조 적용]